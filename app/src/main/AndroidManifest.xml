<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.kidd">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".GameSelectorActivity" />
        <activity android:name=".MakeWordActivity" />
        <activity android:name=".LevelSelectorMakeWordActivity" />
        <activity android:name=".GameCompleteActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".secActivity" />
        <activity android:name=".thirdActivity" />
        <activity android:name=".fourthActivity" />
        <activity android:name=".fifthActivity" />
        <activity android:name=".sixthActivity" />
        <activity android:name=".seventhActivity" />
        <activity android:name=".eightActivity" />
        <activity android:name=".LevelSelectorSpotWordActivity"/>
    </application>

</manifest>